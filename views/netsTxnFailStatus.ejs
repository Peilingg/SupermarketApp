<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
  <title>Payment Failed</title>
  <style>
    body {
      background: #f7f8fa;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }
    .failure-container {
      text-align: center;
      max-width: 500px;
      width: 100%;
      padding: 20px;
    }
    .failure-icon {
      font-size: 72px;
      color: #dc3545;
      margin-bottom: 20px;
    }
    .failure-card {
      border: none;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      padding: 40px 30px;
      background: white;
    }
    .failure-title {
      font-size: 28px;
      font-weight: 600;
      color: #333;
      margin-bottom: 10px;
    }
    .failure-message {
      color: #6c757d;
      margin-bottom: 30px;
      font-size: 16px;
    }
    .button-group {
      display: flex;
      gap: 10px;
      justify-content: center;
      flex-wrap: wrap;
    }
    .btn-custom {
      min-width: 180px;
      padding: 10px 20px;
      font-size: 16px;
      font-weight: 500;
      border-radius: 6px;
    }
  </style>
</head>
<body>
  <div class="failure-container">
    <div class="failure-card">
      <!-- Failure Icon -->
      <div class="failure-icon">
        <i class="bi bi-x-circle-fill"></i>
      </div>

      <!-- Title and Message -->
      <h1 class="failure-title">Payment Failed</h1>
      <p class="failure-message">
        Unfortunately, your NETS QR payment could not be processed. Please try another payment method.
      </p>

      <!-- Error Details (if provided) -->
      <% if (errorMsg || responseCode) { %>
        <div class="alert alert-danger" role="alert">
          <small>
            <% if (errorMsg) { %>
              <strong>Error:</strong> <%= errorMsg %>
            <% } %>
            <% if (responseCode) { %>
              <div class="mt-2"><strong>Response Code:</strong> <%= responseCode %></div>
            <% } %>
          </small>
        </div>
      <% } %>

      <!-- Action Buttons -->
      <div class="button-group">
        <a href="/checkout" class="btn btn-primary btn-custom">
          <i class="bi bi-arrow-left"></i> Try Another Payment Method
        </a>
        <a href="/shopping" class="btn btn-outline-secondary btn-custom">
          <i class="bi bi-bag-check"></i> Continue Shopping
        </a>
      </div>
    </div>
  </div>
</body>
</html>
