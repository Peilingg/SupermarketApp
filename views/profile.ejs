<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Supermarket App</title>
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container">
        <h2 class="mt-4">User Profile</h2>
        <div class="card mt-3" style="max-width: 400px;">  
            <div class="card-body">
                <h5 class="card-title"><strong>Full Name:</strong><%= users.username %></h5>
                <p class="card-text"><strong>Email:</strong> <%= users.email %></p>
                <p class="card-text"><strong>Address:</strong> <%= users.address %></p>
                <p class="card-text"><strong>Contact Number:</strong> <%= users.contact %></p>
                <p class="card-text"><strong>Role:</strong> <%= users.role %></p>
            </div>
        </div>
    </div>
</body>
</html>