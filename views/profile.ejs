<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <title>Profile</title>
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container my-5">
    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } else if (!user) { %>
      <div class="alert alert-warning">User not found.</div>
    <% } else { %>
    <p class="text-center" style="font-size:35px;"><strong>My Profile </strong></p>
      <div class="card mx-auto" style="max-width:720px;">
        <div class="card-body">
          <div class="d-flex flex-column align-items-center text-center">
            <div>
              <img src="/images/profile.png" alt="Profile" width="64">
            </div>

            <h4 class="mt-3 mb-1"><%= user.username %></h4>
            <span class="badge bg-secondary text-white"><%= user.role %></span>
          </div>

          <hr />

          <div class="row mt-3">
            <div class="col-md-6 mb-2"><strong>Full Name</strong><div class="form-control-plaintext"><%= user.username %></div></div>
            <div class="col-md-6 mb-2"><strong>Email</strong><div class="form-control-plaintext"><%= user.email %></div></div>
            <div class="col-md-6 mb-2"><strong>Address</strong><div class="form-control-plaintext"><%= user.address || '-' %></div></div>
            <div class="col-md-6 mb-2"><strong>Contact</strong><div class="form-control-plaintext"><%= user.contact || '-' %></div></div>
          </div>

          <div class="mt-3 text-center">
            <% if (isOwner) { %>
              <a href="/profile/edit" class="btn btn-primary">Edit Profile</a>
            <% } else if (isAdmin) { %>
              <a href="/users/<%= user.userId %>/edit" class="btn btn-primary">Edit Profile</a>
            <% } %>
          </div>
        </div>
      </div>
    <% } %>
  </div>
</body>
</html>